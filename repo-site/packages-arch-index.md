---
layout: repo-index
title: "packages — __TREE__ — __ARCH__ — trusttunnel-openwrt"
---

# packages — `__ARCH__`

Signed package feeds for the `__ARCH__` architecture in the OpenWrt
`__TREE__` release tree. A device uses only its own subdirectory — apk
matches the package arch against the device's own arch exactly, and the
opkg feed line names the same directory.

Feeds:

__FEED_LIST__

[« back to the packages repository](../)
