#!/bin/sh
# Lab stub: the diagnose route check greps for "dev tun0".
case "$1 $2 $3 $4" in
	"route show table"*) echo "default dev tun0 table 880 metric 1" ;;
	"route show default"*) echo "default via 192.168.1.1 dev br-lan" ;;
esac
exit 0
